import Layout from "@/src/components/Layout";

export default function ClienteProCodigo(){
    return (
        <Layout titulo="Navegação Dinâmica">
            <span> Código = ??? </span>
        </Layout>
    )
}

// navegação dinâmica = qualquer coisa que tenha depois de /cliente vai cair aqui